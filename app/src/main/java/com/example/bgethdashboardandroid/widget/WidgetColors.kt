package com.example.bgethdashboardandroid.widget

import androidx.compose.ui.graphics.Color

object WidgetColors {
    // Background gradient colors (used in drawable)
    val BackgroundStart = Color(0xFF0D0D1E)
    val BackgroundMid = Color(0xFF14142E)
    val BackgroundEnd = Color(0xFF1E1A38)

    // Text colors
    val PrimaryText = Color.White
    val SecondaryText = Color.White.copy(alpha = 0.8f)
    val TertiaryText = Color.White.copy(alpha = 0.4f)
    val LabelColor = Color.White.copy(alpha = 0.6f)

    // Accent colors
    val CyanAccent = Color(0xFF00BCD4)
    val LogoTint = Color.White.copy(alpha = 0.8f)

    // UI elements
    val DividerColor = Color.White.copy(alpha = 0.15f)
    val InactiveBar = Color.White.copy(alpha = 0.2f)
}
