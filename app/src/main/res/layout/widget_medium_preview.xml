<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:padding="20dp">

    <!-- BG Castle Logo top right -->
    <ImageView
        android:id="@+id/logo"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:src="@drawable/ic_bg_logo" />

    <!-- Divider in center -->
    <ImageView
        android:id="@+id/divider"
        android:layout_width="1dp"
        android:layout_height="80dp"
        android:layout_centerInParent="true"
        android:src="@drawable/divider_line" />

    <!-- Left column - ETH (centered in left half) -->
    <LinearLayout
        android:id="@+id/left_column"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@id/divider"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <TextView
            android:id="@+id/label_ethereum"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Ethereum"
            android:textColor="#99FFFFFF"
            android:textSize="14sp"
            android:fontFamily="sans-serif-medium" />

        <TextView
            android:id="@+id/eth_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="3.093 $"
            android:textColor="#FFFFFF"
            android:textSize="36sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium" />

        <TextView
            android:id="@+id/timestamp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="12:00"
            android:textColor="#66FFFFFF"
            android:textSize="13sp"
            android:fontFamily="sans-serif-medium" />
    </LinearLayout>

    <!-- Right column - Gas (centered in right half) -->
    <LinearLayout
        android:id="@+id/right_column"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@id/divider"
        android:layout_alignParentEnd="true"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <LinearLayout
            android:id="@+id/gas_row"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@drawable/ic_gas_pump"
                android:tint="#00BCD4" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:text="Gas"
                android:textColor="#99FFFFFF"
                android:textSize="14sp"
                android:fontFamily="sans-serif-medium" />
        </LinearLayout>

        <TextView
            android:id="@+id/gas_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="0.030"
            android:textColor="#FFFFFF"
            android:textSize="32sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium" />

        <TextView
            android:id="@+id/gas_unit"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="gwei"
            android:textColor="#99FFFFFF"
            android:textSize="14sp"
            android:fontFamily="sans-serif-medium" />

        <!-- Gas level indicator using ImageViews -->
        <LinearLayout
            android:id="@+id/gas_indicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="12dp"
                android:layout_height="6dp"
                android:src="@drawable/gas_bar_active" />

            <ImageView
                android:layout_width="12dp"
                android:layout_height="6dp"
                android:layout_marginStart="5dp"
                android:src="@drawable/gas_bar_inactive" />

            <ImageView
                android:layout_width="12dp"
                android:layout_height="6dp"
                android:layout_marginStart="5dp"
                android:src="@drawable/gas_bar_inactive" />

            <ImageView
                android:layout_width="12dp"
                android:layout_height="6dp"
                android:layout_marginStart="5dp"
                android:src="@drawable/gas_bar_inactive" />

            <ImageView
                android:layout_width="12dp"
                android:layout_height="6dp"
                android:layout_marginStart="5dp"
                android:src="@drawable/gas_bar_inactive" />
        </LinearLayout>
    </LinearLayout>

    <!-- Refresh button bottom right -->
    <ImageView
        android:id="@+id/refresh"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:src="@drawable/ic_refresh"
        android:tint="#66FFFFFF" />

</RelativeLayout>
